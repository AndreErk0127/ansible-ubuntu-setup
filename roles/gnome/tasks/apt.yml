---
- name: Install apt packages
  apt:
    name: "{{ packages }}"
    force_apt_get: yes
  vars:
    packages:
    - chrome-gnome-shell
    - dconf-editor
    - gnome-shell-extensions
    - gnome-tweaks
